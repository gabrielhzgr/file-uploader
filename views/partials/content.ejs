<% if(content.type=='folder'){ %>
<div id="<%= content.id %>" class="content folder drop-zone uploaded" ondragover="dragOverHandler(event)" ondrop="dropHandler(event)" ondragleave="dragLeaveHandler(event)" onclick="window.location.href = `/storage/<%= content.id %>/`">
  <span class="material-symbols-outlined icon">
    folder
  </span>
  <span class="name">
    <%= content.name %>
  </span>
</div>
<% }else if(content.type.startsWith('image')){%>
<div id="<%= content.id %>" class="content file image uploaded">
  <span class="material-symbols-outlined icon">
    image
  </span>
  <span class="name">
    <%= content.name %>
  </span>
</div>
<% }else if(content.type.startsWith('video')){ %>
<div id="<%= content.id %>" class="content file video uploaded">
  <span class="material-symbols-outlined icon">
    theaters
  </span>
  <span class="name">
    <%= content.name %>
  </span>
</div>
<% }else{ %>
<div id="<%= content.id %>" class="content file other uploaded" onclick="window.location.href = `/storage/<%= content.id %>/`">
  <span class="material-symbols-outlined icon">
    article
  </span>
  <span class="name">
    <%= content.name %>
  </span>
</div>
<% } %>